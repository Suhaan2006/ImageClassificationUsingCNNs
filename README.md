# ImageClassificationUsingCNNs
The objective of this project is to develop a DL model for multi-class image classification using a CNN.
The model is trained on the CIFAR-10 dataset.
The performance of the model is evaluated using standard classification metrics such as accuracy, precision, recall, F1-score, confusion matrix, and ROC curve.
